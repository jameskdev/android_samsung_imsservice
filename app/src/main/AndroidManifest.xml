<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Imsservice">

        <service android:enabled="true" android:exported="true" android:name="com.sec.internal.google.GoogleImsServiceAdapter" android:permission="android.permission.BIND_IMS_SERVICE" android:singleUser="true">
            <meta-data android:name="android.telephony.ims.MMTEL_FEATURE" android:value="true" />
            <intent-filter>
                <action android:name="android.telephony.ims.compat.ImsService" />
            </intent-filter>
        </service>

    </application>

</manifest>